# 阶段9 第一轮：开发启动清单

最后更新: 2026-03-22  
状态: Done（第一轮历史清单，保留用于追溯）

## 文档定位

这份清单记录的是阶段9第一轮刚开工时的启动条件、任务包顺序和联调闸门。

当前阶段9已经完成双资产 V2 收官，因此本文件只作为历史参考，不再代表当前主计划或当前验收依据。

## 第一轮历史启动条件

当时的开工基线是：

- `strategy-engine` 可启动
- Go 后端已配置 `STRATEGY_ENGINE_BASE_URL`
- Admin 可启动
- MySQL 可执行 `backend/migrations`
- 阶段8股票真实上下文主链已稳定
- 旧 `MarketCenter` 入口必须保留

## 第一轮历史任务包

第一轮历史上按如下顺序推进：

1. 包0：基线校验与目录准备
2. 包1：协议与数据库骨架
3. 包2：Python 自动选股主链
4. 包3：Go 后端持久化与 Admin API
5. 包4：Admin 独立模块骨架
6. 包5：审核发布闭环与迁移期兜底

## 第一轮历史验收焦点

当时只关注：

- `AUTO` 模式下股票自动选股可运行
- `智能选股` 菜单和四页骨架可访问
- profile / run / review / publish 最小闭环可用
- 旧客户端和旧后台入口不回归

## 当前使用方式

如果需要追溯“第一轮为什么这样分包、为什么先做 stock-only、为什么当时只保留四页骨架”，可以继续参考本文件。

如果要继续实现或验收当前阶段9，请改看：

- `docs/strategy-engine/README.md`
- `docs/strategy-engine/阶段9-股票智能量化选股系统与后台独立模块.md`

## 默认约定

- 本文件不再继续补新的任务包或验收条件
- 当前阶段9的任何新增实现，不应再回写到这份历史启动清单中
